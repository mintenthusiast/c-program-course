#ifndef MYARRAY_H
#define MYARRAY_H

/*
 * This is a list of the functions that 
 * I developed to handle arrays.
 *
 */

int sort_array(int *a, int len); //sorts array

int min_loc_swap(int *a, int len); //swaps min with the first term

int print_array(int *a, int len); //prints array

#endif
